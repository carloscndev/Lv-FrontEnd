// Import Modules
import React from 'react'

// Import styles
import './styles.scss'

const MaxContent = ({ children }) => (
  <div className='max-content'>
    {children}
  </div>
)

export default MaxContent
